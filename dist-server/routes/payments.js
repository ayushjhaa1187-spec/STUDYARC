import { Router } from 'express';
const router = Router();
// Mounted at /api/payments
// Placeholder for future payment logic (like creating an order)
// router.post('/create-order', ...);
export default router;
